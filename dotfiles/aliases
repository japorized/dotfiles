{%@@ if profile == "Earl-Grey.local" @@%}
##############################
# Eye saving commands
##############################
alias day="~/.bin/change-wallpaper ~/Pictures/Wallpaper/konohanakitan_hydrangea.jpg"
alias night="~/.bin/change-wallpaper ~/Pictures/Wallpaper/powerarrow_bg.png"
{%@@ endif @@%}

##############################
# Navigation
##############################
alias lsdir="ls -d */"
{%@@ if profile == "ubuntu1604-002" @@%}
alias mc.skin='mc --skin="modarcon16root-defbg"' 
{%@@ endif @@%}

{%@@ if profile == "Earl-Grey.local" @@%}
##############################
# Homebrew (MacOS)
##############################
alias listBrewDependents='brew list -1 | while read cask; do echo -ne "\x1B[1;34m $cask \x1B[0m"; brew uses $cask --installed | awk '\''{printf(" %s ", $0)}'\''; echo ""; done'
{%@@ endif @@%}


##############################
# Vim
##############################
{%@@ if profile == "Earl-Grey.local" @@%}
alias vim="/usr/local/bin/vim "
{%@@ endif @@%}
alias vi="vim "
alias view="vim -R "

##############################
# Others
##############################
alias magit="vim -c MagitOnly"
{%@@ if profile == "Earl-Grey.local" @@%}
alias aria2c-rpc="aria2c --enable-rpc --rpc-listen-all -d './Downloads' "
{%@@ endif @@%}
alias gdi="fuck"
{%@@ if profile == "Oolong" @@%}
alias volume="pactl set-sink-volume 0 "
{%@@ endif @@%}
